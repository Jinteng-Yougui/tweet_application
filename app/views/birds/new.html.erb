<table>
  <%= form_with(model: @bird, local: true) do |form| %>
    <div class="bird_posts">
    <%= form.label :content %>
    <%= form.text_field :content%>
    </div>
  <%= form.submit %>
  <% end %>
</table>

